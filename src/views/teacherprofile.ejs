<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Teacher Profile</title>

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
      font-family: "Poppins", sans-serif;
    }

    body{
      background:#f1f3f6;
      padding:40px;
      display:flex;
      justify-content:center;
      align-items:flex-start;
    }

    .profile-card{
      width:100%;
      max-width:650px;
      background:white;
      padding:30px;
      border-radius:18px;
      box-shadow:0 5px 20px rgba(0,0,0,0.1);
    }

    .profile-card h2{
      text-align:center;
      margin-bottom:20px;
      font-size:28px;
      color:#2d3436;
    }

    .profile-item{
      margin-bottom:15px;
      font-size:17px;
    }

    .profile-item span{
      font-weight:600;
      color:#0984e3;
    }

    .back-btn{
      display:inline-block;
      margin-top:20px;
      padding:10px 18px;
      background:#0984e3;
      color:white;
      border-radius:8px;
      text-decoration:none;
    }

    .back-btn:hover{
      background:#0a6ec4;
    }

  </style>
</head>

<body>

  <div class="profile-card">
    <h2>Teacher Profile</h2>

    <div class="profile-item">
      <span>Name:</span> <%= teacher.name %>
    </div>

    <div class="profile-item">
      <span>Email:</span> <%= teacher.email %>
    </div>

    <div class="profile-item">
      <span>Teacher ID:</span> <%= teacher.id %>
    </div>

    <div class="profile-item">
      <span>Subject:</span> <%= teacher.subject %>
    </div>

    <div class="profile-item">
      <span>Class Assigned:</span> <%= teacher.class %>
    </div>

    <div class="profile-item">
      <span>Salary:</span> ₹<%= teacher.salary %>
    </div>

    <div class="profile-item">
      <span>Contact No:</span> <%= teacher.contactNo %>
    </div>

    <div class="profile-item">
      <span>Address:</span> <%= teacher.address %>
    </div>

  </div>

</body>
</html>
